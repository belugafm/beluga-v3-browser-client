const config: {
    server: {
        domain: string
        https: boolean
    }
    site_name: string
} = {
    server: {
        domain: "localhost.beluga.fm",
        https: false,
    },
    site_name: "Beluga",
}

export default config
